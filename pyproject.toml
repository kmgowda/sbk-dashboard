[build-system]
requires = ["setuptools>=68,<81", "wheel>=0.43,<1"]
build-backend = "setuptools.build_meta"

[project]
name = "sbk-dashboard"
version = "1.0.0"
description = "SBK/SBM Grafana dashboard control server"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
authors = [{name = "KMG"}]
dependencies = ["psutil>=5.9,<8"]

[project.optional-dependencies]
dev = [
  "build>=1.2,<2",
  "coverage[toml]>=7.6,<8",
  "mypy>=1.14,<2",
  "pytest>=8.3,<10",
  "ruff>=0.9,<1",
  "setuptools>=68,<81",
  "types-psutil>=7.0,<8",
  "wheel>=0.43,<1",
]

[project.scripts]
sbk-dashboard = "sbk_dashboard.main:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
sbk_dashboard = [
  "resources/monitoring-download.properties",
  "resources/grafana/dashboards/sbk-dashboard.json",
  "resources/web/*.html",
  "resources/web/*.css",
  "resources/web/*.js",
]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["sbk_dashboard"]

[tool.coverage.report]
show_missing = true
fail_under = 60

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "C4"]

[tool.mypy]
python_version = "3.10"
files = ["src"]
check_untyped_defs = true
disallow_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
